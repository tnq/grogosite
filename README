Wow we have a readme now

# Setting Stuff up
When you clone this repo, run "git clone --recursive ...".
Check that localwiki is at commit "a0e8fa8", if it's not, run "git reset --hard a0e8fa8"
make sure you have mysql installed ,if not, run "brew install mysql"
